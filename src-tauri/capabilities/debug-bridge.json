{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "debug-bridge",
  "description": "Dev-only HTTP/WS bridge for the `tauri-browser` CLI to inspect and drive the running app. The plugin's server is gated upstream by `#[cfg(debug_assertions)]`, so `npm run tauri build` (release) compiles out the server even though the crate is linked and this capability is registered.",
  "windows": ["main"],
  "permissions": ["debug-bridge:default"]
}
